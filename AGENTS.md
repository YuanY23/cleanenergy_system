# 项目 AI 使用规则

本文件适用于仓库中的全部 AI 助手、自动化代理和后续维护者。

## 强制阅读顺序

在解释项目、引用数据、修改报告或生成简历材料前，必须依次读取：

1. `交付成果/最新项目/README.md`
2. `交付成果/最新项目/LATEST_PROJECT.json`
3. `交付成果/最新项目/项目交付说明.md`
4. 与任务有关时再读 `项目研究报告.md`、`结果与指标参考.md`、`复现与更新指南.md`

## 当前唯一有效基线

- 正式运行 ID：`formal-2024-20260809T131238Z`
- 模型代码提交：`34a40e7c11bad8b09c7d48c5c37d489151dfdc3b`
- 完成状态：`complete`
- 结果目录：`artifacts/runs/formal-2024-20260809T131238Z/`
- 指针：`artifacts/latest.json`

如果 `artifacts/latest.json` 指向新的、状态为 `complete` 的运行，应先更新 `LATEST_PROJECT.json` 及整套最新文档，再使用新结果。不得只替换部分数字。

## 禁止作为当前结论的内容

- `archive/` 下全部文件；
- `交付成果/历史结果归档/` 下全部文件；
- 根目录旧 Excel 数据包；
- 旧 S0—S5、24 小时示例、旧典型日、旧 NASA 替代数据生成的结果；
- `archive/failed_runs/` 中的失败运行。

上述内容只能用于历史追溯，不得用于当前报告、答辩、简历或结果比较。

## 表述纪律

1. 所有量化结果必须同时记录 `run_id`，并可回溯到 `metrics/`、`planning/`、`replay/` 或 `reliability/` 下的原始 CSV。
2. 园区负荷是公开尺度校准的合成数据，不得称为企业 SCADA 实测。
3. ERA5 是生产气象，NASA POWER 只做独立月度交叉校核。
4. 位置法与零碳园区核算方法必须分列，外送电不自动抵扣运行排放。
5. 低碳型是时间上限内的可行解，实际 MIP gap 为 1.335%，不得宣称严格全局最优。
6. 可靠性是 54 个确定性设计事件结果，不得写成 EENS、LOLP、SAIDI、SAIFI 或认证结论。
7. “120% 保安负荷”和 4 小时电池是工程对标约束，不是规范认证。
8. API 密钥、账号和本机凭据不得写入仓库、报告、日志或对外材料。

## 更新规则

生成下一版正式成果时必须使用全新 `run_id`，通过清单哈希、8,784 小时时轴、求解、回放、可靠性和报告完整性检查后，原子更新 `artifacts/latest.json`。随后同步更新本文件、`LATEST_PROJECT.json`、项目研究报告、项目介绍、结果参考和根 README；旧正式版本转入历史摘要，不覆盖、不混算。
