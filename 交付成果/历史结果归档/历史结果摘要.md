# 历史结果摘要

状态：**仅供追溯，禁止作为当前项目结论**

当前有效成果入口：[最新项目](../最新项目/README.md)

## 1. 归档目的

本目录不复制旧数据进入新研究，也不删除原始证据，只对被淘汰的内容进行统一登记。任何 AI、简历、报告或面试材料都不得从本页所列历史路径引用当前数字。

## 2. 旧版生成结果

`archive/legacy_generated/2026-08-09/` 保存早期 `outputs/`、`results_v1/` 和历史 `data_processed/`。主要内容包括：

- 旧 24 小时/S0—S5 调度结果；
- 旧典型日和早期年化结果；
- 旧容量规划、投资敏感性、Pareto、随机/鲁棒规划结果；
- 基于旧输入或旧方法生成的图、表、PDF 和 Excel。

淘汰原因：这些成果不属于当前 manifest 固定的 ERA5 8,784 小时研究链，结果口径、输入版本和模型结构与当前项目不同，混用会造成数据污染。

## 3. 旧版研究文档

`archive/legacy_docs/2026-08-09/` 保存旧《研究论文》《项目介绍书》《数据来源汇总》《项目模型代码解析》等文档。它们依赖旧数据或旧场景叙事，已被全新的[项目研究报告](../最新项目/项目研究报告.md)和[项目介绍](../最新项目/项目介绍.md)替代。

原 `项目总结汇总/` 中的岗位能力证据卡、待正式结果简历模板和秋招版项目介绍，已迁移到本目录的 `旧版项目总结/` 子目录，只用于观察版本演进，不能继续填写或投递。

## 4. 失败正式运行

`archive/failed_runs/` 保存三次失败运行现场：

| 运行目录 | 失败原因 | 是否有正式结论 |
| --- | --- | --- |
| `formal-2024-20260809T112605Z_manifest-name-mismatch` | manifest 与运行目录命名不一致 | 否 |
| `formal-2024-20260809T112724Z-reliability-terminal-state` | 可靠性事件终端状态处理错误 | 否 |
| `formal-2024-20260809T122510Z-replay-negative-zero` | 回放状态出现负零数值边界问题 | 否 |

这些目录用于证明问题被发现和隔离，不得与成功运行合并。相关问题修复后，最终成功运行是 `formal-2024-20260809T131238Z`。

## 5. 根目录旧 Excel

- `第一组数据汇总表.xlsx`
- `鄂尔多斯零碳园区_电热氢储优化调度_数据包.xlsx`

两份文件未删除，以保留用户资料；正式 manifest 已在 `excluded_history_paths` 中明确排除它们。当前代码和报告不得自动读取这两份文件。

## 6. 历史内容使用规则

历史文件只允许用于：版本追溯、排错、说明为什么改进方法。若要重新启用其中任何数据或模型，必须把它当作新研究输入，重新核验来源、单位、适用期和 SHA256，并使用全新 run_id 完整重跑；不得直接复制历史结论到最新目录。
